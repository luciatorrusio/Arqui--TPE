#include "./include/Game.h"

int MIDDLE = SCREEN_WIDTH/2;
int NBLOQUES = 20; //Cantidad de bloques
int runGame(void){
    int bloques[NBLOQUES];
    int status[]={MIDDLE, ALL_BLOCKS, } //Donde status= {pos_barra(x), matrizDeBloques(si esta en 1, sino en 0), pos de pelota(x,y)}

}